[pytest]
testpaths = tests
; The `cmd` package at the repository root shadows the standard library `cmd`
; module, which `pdb` imports. Disabling the debugging plugin keeps the suite
; runnable from the repository root.
addopts = -p no:debugging
