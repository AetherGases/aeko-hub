MONGO_URI=
DB_NAME=
PORT=
HOST=

# Aeko SDK. The SDK never reads the environment itself: cmd/api/main.py reads
# these and hands them to Aeko.config() once, at startup.
GEMINI_API_KEY=
# Optional. Left empty, the SDK defaults are used.
AEKO_FAST_MODEL=
AEKO_SLOW_MODEL=
AEKO_MAX_TOKENS=
AEKO_REPORT_MAX_TOKENS=
